# Simple Python QR Generator

A minimalist and functional Python script to generate QR codes from links or text.

## Requirements

Ensure you have Python installed, then install the necessary library using:

```bash
python -m pip install qrcode[pil]
